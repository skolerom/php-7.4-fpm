# =============================================================================
# Docker Image Configuration
# =============================================================================

# Image name (without registry prefix)
# For GHCR, this should be: <github-org-or-user>/<image-name>
IMAGE_NAME=skolerom/php-74-fpm

# Image tag
IMAGE_TAG=latest

# Dockerfile to use
DOCKERFILE=Dockerfile

# =============================================================================
# Container Registry Configuration
# =============================================================================

# Container registry URL
# Examples:
#   - ghcr.io (GitHub Container Registry)
#   - docker.io (Docker Hub)
#   - <account>.azurecr.io (Azure Container Registry)
CONTAINER_REGISTRY=ghcr.io

# =============================================================================
# Build Options
# =============================================================================

# Additional build arguments (space-separated)
# Example: BUILD_ARGS=--build-arg VERSION=1.0 --build-arg DEBUG=true
BUILD_ARGS=

# Set to 'true' to build without cache
NO_CACHE=false

# Default platform for builds
PLATFORM=linux/amd64

# =============================================================================
# GitHub Container Registry Authentication
# These can also be set as environment variables for security
# =============================================================================

# GitHub username (required for GHCR login)
# GITHUB_USER=your-github-username

# GitHub Personal Access Token with 'write:packages' scope
# WARNING: Do not commit tokens to version control!
# GITHUB_TOKEN=ghp_xxxxxxxxxxxx
